import styled from '@emotion/styled';
import { Login } from '@emourinho/react-authenticator';

const StyledApp = styled.div`
  // Your style here
`;

export function App() {
  return (
    <StyledApp>
      <Login />
    </StyledApp>
  );
}

export default App;
